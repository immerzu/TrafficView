# HANDOVER 2026-08-19 (v1.4.38) — TrafficView

Session: Über-Dialog-Layout-Fix (Labels sichtbar) + Patch-Release v1.4.38 (Bump, Tests, Portable-Build, Tag, GitHub-Release, Gedächtnis-Update).

## Endzustand (verifiziert 2026-08-19)

- **HEAD / main / origin/main:** `e0bc07b` („chore: bump version to 1.4.38"), Arbeitsbaum **sauber**.
- **Tags:** `v1.4.33` → `fd3247a`; `v1.4.34` → `18e9c90`; `v1.4.35` → `9e97b5f`; `v1.4.36` → `cd7b056`; `v1.4.37` → `2d37e56`; `v1.4.38` → `e0bc07b` (annotiert, gepusht).
- **Version:** 1.4.38 (README, README_EN, AssemblyInfo 1.4.38.0, Manual.txt, dist/Manual.txt, dist/README.md — konsistent, keine Reste).
- **GitHub-Release:** „TrafficView v1.4.38" (https://github.com/immerzu/TrafficView/releases/tag/v1.4.38), **2 Assets**: `TrafficView_Portable_1.4.38.zip` (507.347 B) + `.zip.sha256` (99 B).
- **Portable-ZIP:** `F:\001_Coding_Projekte\TrafficView_Moi\01_Ausgabe\TrafficView_Portable_1.4.38.zip` — SHA-256 `73adcead13d1e6456f23e6b01e1eff9233e7ec71824ce327e2bc9476b364d6d7` (= `.sha256`), Manifest `version 1.4.38` / `commit e0bc07b…` (Build nach Release-Commit).

## Über-Dialog-Layout-Fix (Ursache belegt)

- **Symptom (Nutzer):** nur kleines, rechts verschobenes Logo, keine Texte.
- **Ursache:** Falsche Dock-Z-Order in `ShowCompanyLogoWindow` (`src/TrafficViewContext.Branding.cs`): `containerPanel.Controls.Add(logoArea)` VOR `Add(infoArea)` — WinForms dockt in umgekehrter Z-Order, `infoArea` (Fill) belegte die ganze Fläche, `logoArea` (Top) verdeckte den oberen Bereich mit den Labels. Zusätzlich keine explizite `ForeColor`.
- **Fix (`008cb2c`):** `Add(infoArea)` vor `Add(logoArea)`; Labels in `infoArea` in Reihenfolge `copyright → version → name` einfügen (Name oben); `ForeColor = Color.Black` auf allen drei Labels. Neue Helfer `GetAssemblyProductName()`/`GetAssemblyCopyrightText()` (zentrale Attribut-Quellen), Version via `GetMenuVersionText()`. Unverändert: Sizable, Min 320×320, Max 90 % WorkingArea, X/ESC, ShowDialog ohne Owner.

## Release-Ablauf (etablierte Vorlage, wiederverwendet)

1. Ist-Check → `Run-AllTests.ps1` grün → Bump `1.4.38` (`Bump-Version.ps1 -AllowDirtyWorkingTree` + `sed 1.4.37→1.4.38` auf README/EN/Manual/dist-Manual + `cp README.md dist/README.md`) → Konsistenz/Reste.
2. Release-Commit (6 Versionsdateien, EIN Commit) → Push → SHA notieren.
3. `Create-PortableRelease.ps1 -OutputRoot F:\001_Coding_Projekte\TrafficView_Moi\01_Ausgabe` NACH dem Commit.
4. Manifest/SHA prüfen → `git tag -a v1.4.38 -m "Release v1.4.38"` + Push → `gh release create v1.4.38 <zip> <sha256> --title … --notes-file …` → `gh release view` verifizieren.
5. memory/FACT.md + HANDOVER-Update → Commit + Push.

## Wichtige Regeln (unverändert gültig)

- Bump-Lücke: Manual.txt + dist/Manual.txt manuell; dist/README.md aus Root kopieren.
- Manifest-Timing: Build IMMER nach dem Release-Commit.
- Dauerregel: Ausgabe nach `01_Ausgabe` (-OutputRoot).
- dist/TrafficView.exe und dist/Verbrauch.archiv.*.txt.gz sind gitignored (unangetastet).
- memory/ ist versioniert; HANDOVER pro Release als Momentaufnahme.

## Offene Punkte

- **Manuelle UI-Abnahme Über-Dialog v1.4.38** (Nutzer): Logo mittig oben; darunter „TrafficView", „Version 1.4.38", „Copyright (c) 2026"; Verkleinern per Rahmen (min. 320×320), X und ESC schließen. Ergebnis in `docs/manual-test-log.md` dokumentieren.
- H2 (F:\Codex-Pfade in DisplayModeAssetSources), H6 (README_EN unvollständig), H7 (OutputRoot-Default), H8 (Bump-Beispiel 1.4.26) — unverändert offen.
- GitHub-Release v1.4.34 hängt noch das erste ZIP (506.817 B, Stand `1de29e2`).

## Nächste Schritte (Vorschlag, NICHT ausgeführt)

1. Nutzer: Über-Dialog-Abnahme + Eintrag in `docs/manual-test-log.md`.
2. H7/H2/H6/H8 bei Gelegenheit; v1.4.34-Asset optional aktualisieren.
